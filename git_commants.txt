1.) git init(initialization)
git status
2.) git add -A(add file to staging medium)
3.) git commit -m "your message" (adding file to local medium)
git status(list all untracked file)

if changes is made then
---------------
git status
2.) git add -A(add file to staging medium)
3.) git commit -m "your message" (adding file to local medium)
git status(list all untracked file)


4.) git remote add spacename path (add remote path)

5.) git remote -v (to check if its ready  to fetch and push)

6.) git push -u spacename master(push to git)
